// Imprimir os números em sequência até o que o usuário escolher (usuário deve escolher entre mostrar pares ou ímpares)

using System ;

    class ParesOuImpares 
    { /*
        static void Main(string[] args)
        {
            Console.Write("Quais números você que ver? (Digite par ou impar): ");
            string escolha = Console.ReadLine() ;
        
            Console.Write("Escolha um número: ");
            int numero = int.Parse(Console.ReadLine());

            int i;

            if(escolha == "par")
            {
                i = 2;
            }

            else if(escolha == "impar")
            {
                i = 1;
            }
            else
            {
                Console.WriteLine("Entrada Inválida, digite outro número");
                return ; // Encerra o programa
            }

            while(i <= numero)
            {
                Console.Write(i + " ") ;
                i += 2 ; // Pula de 2 em 2
            }
        } */
    }